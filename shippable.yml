language:
     - node_js
     - python
# Version number
node_js:
    - 0.12
python:
    - 3.4

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  
  ci:
    - ps -eax
    - ls -al
    #- sleep 7000s

  post_ci:
    #- docker push shippabledocker/sample_node

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
     

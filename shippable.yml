# Language setting
language: node_js

# Version number
node_js:
  - 0.12
addons:
   firefox: "21.0"
   hosts:
    - google.com
    - asdf.com
    
env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  
  ci:
    - ps -eax
    - ls -al
    - firefox --version

  post_ci:
    - docker push shippabledocker/sample_node

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
     
